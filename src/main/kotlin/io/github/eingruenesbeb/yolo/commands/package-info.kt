/**
 * Contains classes, which handle command execution and also the
 * [io.github.eingruenesbeb.yolo.commands.CommandRegistrar], that registers the commands upon loading the plugin.
 */
package io.github.eingruenesbeb.yolo.commands
